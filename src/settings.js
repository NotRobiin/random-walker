// Window width & height.
// For some reason the internal 'windowWidth' and 'windowHeight' did not work properly.
let ww = 800;
let wh = 600;

// Speed multiplier
let walker_speed_multiplier = 2.0;
// Max speed.
let walker_max_speed = 20;
// How many points to save & draw.
let walker_history = 25;
// How many new targets will be created when there are no targets left.
let walker_new_targets = [1, 3];

let target_size = 25;
let target_color = [30, 130, 30];